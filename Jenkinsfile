pipeline {
    agent any
    stages {
        stage('Clone') {
            tools { git 'Git' }
            steps {
                git 'https://github.com/Nang13/jenkins-test.git'
            }
        }
   }
}
